'use strict';

angular.module('qameleonApp.services.leave', ['qameleonApp.services.firebaseRefs'])
    .factory('Leave', ['angularFireCollection', 'FireRef', '$http', '$templateCache',
        function(angularFireCollection, FireRef, $http, $templateCache, $scope) {
            
        }
    ])
